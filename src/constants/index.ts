// 10 minutes
export const FORECAST_CACHE_TIME = 1000 * 60 * 10;

// Brooklyn
export const DEFAULT_LATITUDE = 40.6501;
export const DEFAULT_LONGITUDE = -73.94958;

// % of the window width or height that must be panned before route is changed
export const PANNING_ROUTER_CHANGE = 0.1;
